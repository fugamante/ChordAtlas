# Stage 4 promotion fixture

`fixture-manifest.json` is a CC0 deterministic musical-domain fixture. It contains
only synthetic chord labels, integer frame boundaries, and expected chart
structure. It contains no recording, media bytes, local path, or lyrics.
